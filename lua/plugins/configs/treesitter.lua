local options = {
  ensure_installed = {
    "lua",
    "go",
    "python",
    "rust",
  },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
